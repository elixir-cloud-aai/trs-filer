apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-{{ .Values.trs_filer.appName }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.minio.volumeSize }}
status:
